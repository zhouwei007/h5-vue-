<!DOCTYPE html>
<html lang="zh-cmn-Hans">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta http-equiv="cache-control" content="max-age=0" />
    <meta http-equiv="cache-control" content="no-cache" />
    <meta http-equiv="expires" content="-1">
    <meta http-equiv="expires" content="Tue, 01 Jan 1980 1:00:00 GMT" />
    <meta http-equiv="pragma" content="no-cache">
    <meta content="yes" name="mobile-web-app-capable">
    <meta name="viewport" content="width=device-width,height=device-height,initial-scale=1.0,maximum-scale=1.0,maximum-scale=1.0,user-scalable=no,viewport-fit=cover">
    <meta name="format-detection" content="address=no">
    <link rel="icon" href="<%= BASE_URL %>favicon.ico">
    <title><%= process.env.VUE_APP_TITLE %></title>
    <script type="text/javascript">
      (function () {
        var request = new XMLHttpRequest()
        var t = new Date().valueOf()
        var key = 'saas-yjj-version'
        request.responseType = 'json'
        request.open('GET', '<%= BASE_URL %>version.json?t=' + t, true)
        request.onload = function (data) {
          var last = localStorage.getItem(key)
          if (this.response.version === '<%= htmlWebpackPlugin.options.version %>') {
            last && localStorage.removeItem(key)
          } else if (!last || t - Number(last) > 10000) {
            localStorage.setItem(key, t)
            location.reload(true)
          }
        }
        request.send()
      })()
    </script>
  </head>
  <body>
    <div id="hl-index"></div>
    <noscript>请允许浏览器运行 JavaScript 后使用此 Web 应用</noscript>
  </body>
</html>
